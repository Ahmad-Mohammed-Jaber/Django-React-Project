# Frontend

The frontend uses React with Vite, along with Axios for http requests, to add the JWT token to the request

We also have a navigation, with some routes being protected only for authenticated users, 

The pages use Shadcn components