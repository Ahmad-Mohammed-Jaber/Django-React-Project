# Backend

In the backend here we define our relationships with models, 

We also have serializers, which are responsible for conversion of data from complex model types, into a simple python dictionary and then to JSON

For creating our RESTful apis, we use the Router object from rest_framework, which makes it very easy to do that

